<%- title "Sign in to ZooBit" %>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <%= f.label :login %>

  <%= f.text_field :login, autofocus: true %>


  <%= f.label :password %>

  <%= f.password_field :password, autocomplete: "off" %>


  <% if devise_mapping.rememberable? -%>

    <%= f.check_box :remember_me %> <%= f.label :remember_me %>

  <% end -%>

  <section><%= f.submit "Submit", class: "button small round"%> or <a href="#" class="button small round" data-reveal-id="omniauth"><%=t(:sign_in_media_link)%></a>
  </section>

<% end %>

<%= render "devise/shared/links" %>

<section id="omniauth" class="reveal-modal small" data-reveal>
  <%= render "layouts/omniauthpopup" %>
</section>

<!-- @KL the above is repeated code, can we make this smaller, loke a render or someething? or actually it is a render, it's just the styling, so maybe we can actually include that modal styling in the rendered bit in layouts -->
